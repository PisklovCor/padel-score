# Пользовательские сценарии PadelScore Bot

Документ описывает сценарии использования приложения по разделам: пользователь (поиск турниров, игра с командой) и администратор (создание турниров, статистика). Для каждого раздела — сценарии через кнопки и через команды.

Указание в сценарии: **[не реализовано]** — элемент есть в UI/PRD или в коде (в т.ч. закомментирован), но в текущей версии бота не обрабатывается; **[планируется]** — описан в PRD, в боте пока нет.

---

## 1. Я как пользователь хочу использовать приложение, чтобы находить турниры и играть с командой — через кнопки

### 1.1 Открыть главное меню и перейти к турнирам
- **Шаги:** Запуск бота → кнопка **«🏆 Турниры»**.
- **Ожидание:** Список турниров пользователя с кнопками выбора турнира и «◀️ Назад».
- **Статус:** **[не реализовано]** — кнопка «Турниры» есть, обработчик callback `tournaments` не подключён к списку турниров.

### 1.2 Выбрать турнир и посмотреть команды
- **Шаги:** Турниры → выбор турнира (кнопка «🏆 Название») → кнопка **«👥 Команды»**.
- **Ожидание:** Список команд турнира, кнопки по каждой команде, «➕ Добавить команду», «◀️ Назад».
- **Статус:** **[не реализовано]** — callback `tournament_<id>`, `teams_list_<tournamentId>` не обрабатываются в текущей маршрутизации.

### 1.3 Открыть команду и посмотреть игроков
- **Шаги:** Команды турнира → кнопка команды («👥 Название») → кнопка **«👤 Игроки»**.
- **Ожидание:** Список игроков команды, «➕ Добавить игрока», «◀️ Назад».
- **Статус:** **[не реализовано]** — callback `team_<id>`, `players_list_<teamId>` не обрабатываются.

### 1.4 Посмотреть турнирную таблицу
- **Шаги:** В карточке турнира → кнопка **«📊 Таблица»**.
- **Ожидание:** Турнирная таблица (место, команда, матчи, победы/поражения, сеты, очки).
- **Статус:** **[не реализовано]** — callback `leaderboard_<tournamentId>` не обрабатывается.

### 1.5 Посмотреть матчи турнира
- **Шаги:** В карточке турнира → кнопка **«⚽ Матчи»**.
- **Ожидание:** Список матчей (статус, команда1 vs команда2), «➕ Создать матч», «◀️ Назад».
- **Статус:** **[не реализовано]** — callback `matches_list_<tournamentId>` не обрабатывается.

### 1.6 Открыть матч и посмотреть результат
- **Шаги:** Матчи → выбор матча → для сыгранного матча кнопка **«📊 Просмотр результата»**.
- **Ожидание:** Результат (победитель, счёт, очки).
- **Статус:** **[не реализовано]** — callback `match_<id>`, `match_view_<matchId>` не обрабатываются.

### 1.7 Перейти к «Мои команды»
- **Шаги:** Главное меню → кнопка **«🎭 Мои команды»**.
- **Ожидание:** Список команд пользователя или переход к выбору турнира/команд.
- **Статус:** **[не реализовано]** — кнопка есть, callback `my_teams` не обрабатывается.

### 1.8 Открыть и вести профиль игрока
- **Шаги:** Главное меню → **«👤 Профиль»**.
- **Ожидание:** Данные профиля (ник, имя, рейтинг) или предложение создать профиль; кнопки «📑 Главное меню», «➕ Создать профиль» (если нет профиля), «❓ Помощь по профилю».
- **Статус:** Реализовано (callback `profiles`).

### 1.9 Создать профиль из интерфейса
- **Шаги:** Профиль → **«➕ Создать профиль»** (callback `create_profiles`).
- **Ожидание:** Профиль создаётся по данным Telegram (ник, имя, рейтинг 0).
- **Статус:** Реализовано. Если в Telegram нет username — бот просит использовать команду с ником.

### 1.10 Справка
- **Шаги:** Главное меню → **«❓ Помощь»**.
- **Ожидание:** Краткий список команд и подсказка про inline-кнопки.
- **Статус:** Реализовано (callback `help`).

### 1.11 Вернуться в главное меню
- **Шаги:** В любом экране с кнопкой **«◀️ Назад»** / **«📑 Главное меню»** → нажать её (где callback `main_menu`).
- **Ожидание:** Возврат к главному меню (Турниры, Мои команды, Профиль, Помощь).
- **Статус:** Реализовано для экранов, где есть кнопка с `main_menu` (профиль и др.).

---

## 2. Я как пользователь хочу использовать приложение, чтобы находить турниры и играть с командой — через команды

### 2.1 Старт и приветствие
- **Команда:** `/start`
- **Ожидание:** Приветствие, краткое описание возможностей и главное меню с кнопками; подсказка по созданию турнира.
- **Статус:** Реализовано.

### 2.2 Справка по командам
- **Команда:** `/help`
- **Ожидание:** Список основных команд (/start, /profiles, /help_profiles) и упоминание inline-кнопок.
- **Статус:** Реализовано.

### 2.3 Открыть список турниров
- **Команда:** `/tournaments` или `/my_tournaments`
- **Ожидание:** Список турниров пользователя с возможностью перейти к управлению.
- **Статус:** **[не реализовано]** — `/tournaments` и `/my_teams` указаны в меню бота, но обработчиков команд нет; логика для `/my_tournaments` есть в коде CommandHandler, но отключена.

### 2.4 Открыть «Мои команды»
- **Команда:** `/my_teams`
- **Ожидание:** Список команд пользователя по турнирам или по привязке к пользователю.
- **Статус:** **[не реализовано]** — команда в меню бота, обработчика нет.

### 2.5 Посмотреть свой профиль
- **Команда:** `/profiles`
- **Ожидание:** Профиль (ник, имя, рейтинг) или предложение создать профиль и кнопки меню профиля.
- **Статус:** Реализовано.

### 2.6 Создать профиль
- **Команда:** `/create_profiles` или `/create_profiles Никнейм` (если в Telegram не задан username).
- **Ожидание:** Созданный профиль с ником, именем и рейтингом 0.
- **Статус:** Реализовано.

### 2.7 Обновить профиль
- **Команда:** `/update_profiles`
- **Ожидание:** Возможность изменить данные профиля (имя, рейтинг и т.д.).
- **Статус:** Реализовано (обработчик команды есть).

### 2.8 Удалить профиль
- **Команда:** `/delete_profiles`
- **Ожидание:** Удаление профиля пользователя.
- **Статус:** Реализовано (обработчик команды есть).

### 2.9 Справка по профилю
- **Команда:** `/help_profiles`
- **Ожидание:** Список команд профиля: /profiles, /create_profiles, /update_profiles, /delete_profiles, /help.
- **Статус:** Реализовано.

### 2.10 Присоединиться к турниру по коду
- **Команда:** **[планируется]** например `/join_tournament КОД` или ввод кода в диалоге.
- **Ожидание:** Пользователь попадает в турнир и видит свои команды/матчи.
- **Статус:** **[планируется]** — в PRD есть код приглашения для капитана, в боте не реализовано.

### 2.11 Просмотр таблицы/статистики по турниру
- **Команда:** **[планируется]** например `/leaderboard ID_турнира` или только через кнопки после выбора турнира.
- **Ожидание:** Турнирная таблица в чате.
- **Статус:** **[планируется]** — через команду не реализовано; через кнопки логика есть в коде, но не подключена.

---

## 3. Я как администратор хочу создавать турниры и вести статистику — через кнопки

### 3.1 Создать турнир
- **Шаги:** **[планируется]** Главное меню → кнопка «➕ Создать турнир» (или аналог) → ввод названия/формата в диалоге или следующими кнопками.
- **Ожидание:** Турнир создан, выдан ID/код приглашения.
- **Статус:** **[планируется]** — в главном меню такой кнопки нет; в PRD описан UC-01 (создание турнира по кнопке/команде).

### 3.2 Добавить команду в турнир
- **Шаги:** Турнир → «👥 Команды» → **«➕ Добавить команду»** (callback `team_create_<tournamentId>`). Далее бот может подсказать команду `/add_team ID_турнира Название`.
- **Ожидание:** Команда добавлена в турнир.
- **Статус:** **[не реализовано]** — кнопка и логика подсказки есть в KeyboardUtil/CallbackHandler, но callback не обрабатывается в текущей маршрутизации.

### 3.3 Добавить игрока в команду
- **Шаги:** Команда → «👤 Игроки» → **«➕ Добавить игрока»** (callback `player_create_<teamId>`). Бот подсказывает `/add_player ID_команды Имя Фамилия`.
- **Ожидание:** Игрок добавлен в команду.
- **Статус:** **[не реализовано]** — callback `player_create_*` не обрабатывается.

### 3.4 Создать матч
- **Шаги:** Турнир → «⚽ Матчи» → **«➕ Создать матч»** (callback `match_create_<tournamentId>`). Бот показывает список команд и подсказывает `/add_match ID_турнира ID_команды1 ID_команды2`.
- **Ожидание:** Матч создан, статус «scheduled».
- **Статус:** **[не реализовано]** — callback не обрабатывается.

### 3.5 Ввести результат матча (быстрый)
- **Шаги:** Матчи → матч со статусом «scheduled» → **«📝 Ввести результат»** (callback `match_result_<matchId>`) → выбор счёта **«2-0»** или **«2-1»** (callback `result_quick_<matchId>_2-0` / `_2-1`).
- **Ожидание:** Результат сохранён, таблица и статистика обновлены, матч «completed».
- **Статус:** **[не реализовано]** — callbacks не обрабатываются.

### 3.6 Изменить результат сыгранного матча
- **Шаги:** Матч (completed) → **«✏️ Изменить результат»** (тот же callback `match_result_<matchId>`) → снова выбор 2-0 / 2-1.
- **Ожидание:** Результат обновлён.
- **Статус:** **[не реализовано]** — callback не обрабатывается.

### 3.7 Оспорить результат матча
- **Шаги:** Матч (completed) → **«⚠️ Оспорить результат»** (callback `match_dispute_<matchId>`).
- **Ожидание:** Результат помечен как спорный, админ уведомлён.
- **Статус:** **[не реализовано]** — callback не обрабатывается.

### 3.8 Просмотр турнирной таблицы
- **Шаги:** В карточке турнира → **«📊 Таблица»** (callback `leaderboard_<tournamentId>`).
- **Ожидание:** Таблица с местами, командами, матчами, победами/поражениями, сетами, очками.
- **Статус:** **[не реализовано]** — callback не обрабатывается.

### 3.9 Просмотр списка матчей и деталей матча
- **Шаги:** Турнир → «⚽ Матчи» → выбор матча (callback `match_<id>`).
- **Ожидание:** Карточка матча (команды, статус, формат, дата) и кнопки: ввести результат / просмотр / изменить / оспорить в зависимости от статуса.
- **Статус:** **[не реализовано]** — callbacks не обрабатываются.

### 3.10 Навигация «Назад»
- **Шаги:** В карточке турнира → «◀️ Назад к турнирам» (`tournament_list`); в команде → «◀️ Назад к командам»; в матче → «◀️ Назад к матчам» и т.д.
- **Ожидание:** Возврат на уровень выше без потери контекста.
- **Статус:** **[не реализовано]** — кнопки есть в клавиатурах, соответствующие callbacks не обрабатываются.

---

## 4. Я как администратор хочу создавать турниры и вести статистику — через команды

### 4.1 Создать турнир
- **Команда:** `/create_tournament Название турнира`
- **Ожидание:** Турнир создан, в ответ выдан ID; подсказка использовать /my_tournaments для управления.
- **Статус:** **[не реализовано]** — логика есть в CommandHandler (handleCreateTournament), но вызов отключён (обработчик команды не зарегистрирован).

### 4.2 Показать мои турниры
- **Команда:** `/my_tournaments`
- **Ожидание:** Список турниров пользователя с ID и inline-кнопками для перехода к турниру.
- **Статус:** **[не реализовано]** — логика в CommandHandler есть, обработчик не подключён.

### 4.3 Добавить команду в турнир
- **Команда:** `/add_team ID_турнира Название команды`
- **Пример:** `/add_team 1 Команда А`
- **Ожидание:** Команда создана в указанном турнире, выдан ID команды.
- **Статус:** **[не реализовано]** — логика в CommandHandler есть, вызов отключён.

### 4.4 Добавить игрока в команду
- **Команда:** `/add_player ID_команды Имя Фамилия`
- **Пример:** `/add_player 1 Иван Иванов`
- **Ожидание:** Игрок добавлен в команду, выдан ID.
- **Статус:** **[не реализовано]** — логика в CommandHandler есть, вызов отключён.

### 4.5 Создать матч
- **Команда:** `/add_match ID_турнира ID_команды1 ID_команды2`
- **Пример:** `/add_match 1 1 2`
- **Ожидание:** Матч создан, статус «scheduled».
- **Статус:** **[не реализовано]** — логика в CommandHandler есть, вызов отключён.

### 4.6 Ввести результат матча (детальный)
- **Команда:** **[планируется]** например `/submit_result ID_матча Счёт` или пошаговый ввод сетов/геймов.
- **Ожидание:** Результат сохранён, очки начислены, таблица обновлена.
- **Статус:** **[планируется]** — в боте быстрый ввод только через кнопки 2-0/2-1; детальный ввод по сетам в PRD (UC-06), в командах не описан.

### 4.7 Просмотр таблицы лидеров по API
- **Метод:** GET `/api/v1/tournaments/{tournamentId}/leaderboard`
- **Ожидание:** JSON с рейтингом команд турнира (для админ-инструментов или интеграций).
- **Статус:** Реализовано в REST API; в боте аналогичный сценарий через кнопку «📊 Таблица» не подключён.

### 4.8 Просмотр статистики турнира по API
- **Метод:** GET `/api/v1/tournaments/{tournamentId}/stats`
- **Ожидание:** Общая статистика турнира (количество команд, матчей, таблица и т.д.).
- **Статус:** Реализовано в REST API.

### 4.9 Экспорт таблицы/истории
- **Команда/функция:** **[планируется]** экспорт в CSV/PDF или read-only ссылка (UC-12 в PRD).
- **Статус:** **[планируется]** — в API/боте не реализовано.

---

## Сводка по реализации

| Категория | Через кнопки | Через команды |
|-----------|--------------|----------------|
| **Пользователь: турниры, команды, матчи, таблица** | Кнопки есть, обработка callbacks отключена | /tournaments, /my_teams не обрабатываются |
| **Пользователь: профиль, помощь** | Реализовано (Профиль, Помощь, Создать профиль, Главное меню) | Реализовано (/start, /help, /profiles, /create_profiles, /update_profiles, /delete_profiles, /help_profiles) |
| **Администратор: турниры, команды, игроки, матчи, результаты** | Кнопки и клавиатуры есть, callbacks не обрабатываются | Логика есть в CommandHandler, но не подключена к маршрутизации команд |
| **Администратор: статистика** | Кнопка «📊 Таблица» не подключена | REST API реализован; в боте команд для таблицы нет |

Общая причина «не реализовано» по турнирам/командам/матчам: в CallbackHandler маршрутизация идёт через список `List<Callback>`, в котором обрабатываются только `main_menu`, `help`, `profiles`, `create_profiles`, `help_profiles`. Остальные callback-и (tournaments, tournament_list, tournament_*, teams_list_*, team_*, team_create_*, players_list_*, player_*, player_create_*, matches_list_*, match_*, match_create_*, match_result_*, result_quick_*, match_view_*, match_dispute_*, leaderboard_*) обрабатывались в закомментированном коде и в текущей сборке не выполняются. Команды /create_tournament, /my_tournaments, /add_team, /add_player, /add_match не привязаны к зарегистрированным обработчикам команд.
